(trigger
    (key [[help]]))

(name [View Man Page])

(input nothing)

(output tooltip)

(save nothing)

(script [res=$(ruby -rui -e"print TextMate::UI.request_string(:title => 'Manual Page', :prompt => 'What manual page do you want?', :button1 => 'View', :button2 => 'Cancel').to_s")

[[ -z "$res" ]] && exit_discard

$("$TM_BUNDLE_SUPPORT/mman"<<<"$res")
])